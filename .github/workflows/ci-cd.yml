- name: 🐍 Set up Python 3.10
  uses: actions/setup-python@v4
  with:
    python-version: "3.10"

- name: 📦 Create venv + install deps
  run: |
    python -m venv .venv
    . .venv/bin/activate
    pip install -e ".[dev]"

- name: 🧪 Run tests with coverage
  run: |
    . .venv/bin/activate
    pytest --cov=zenofcode_models tests/
